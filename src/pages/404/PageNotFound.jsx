const PageNotFound = () => {
  return (
    <div
      style={{
        height: "100vh",
        color: "white",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <div className="center">Page not found</div>
    </div>
  );
};

export default PageNotFound;
